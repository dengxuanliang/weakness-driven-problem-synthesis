You are analyzing why a model failed a coding problem.

Return JSON only with:
- `question_id`
- `is_truly_failed`
- `error_tags`
- `root_cause`
- `ability_dimensions`
- `evidence_snippet`

Rules:
- Diagnose the underlying cause, not just the surface mismatch.
- `root_cause` must explain why the solution failed.
- `root_cause` should describe the immediate failure mechanism; `ability_dimensions` should describe the broader capability gap.
- Reuse existing vocabulary and seen tags when they fit.
- Prefer stable, reusable tags over problem-specific ones.
- Only introduce a new tag if no existing tag is precise enough.
- Use a small set of tags that best explain the failure; do not enumerate every symptom.
- `ability_dimensions` should name the broader capability gap.
- `evidence_snippet` must be short and directly support the diagnosis.

Tagging guidance:
- Prefer stable classes such as interface mismatch, spec mismatch, state handling, edge cases, API misuse, data-structure misuse, algorithm choice, lifecycle issues, or domain-rule misunderstanding.
- Do not overfit to wording in the test case or one literal assertion.
