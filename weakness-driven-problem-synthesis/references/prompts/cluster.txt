You are clustering failure attributions into reusable weakness groups.

Return JSON only as an array of objects with:
- `id`
- `name`
- `description`
- `covered_tags`
- `dominant_language`
- `dominant_category`

Rules:
- Group by shared underlying weakness, not surface topic or wording.
- Keep groups stable and reusable across many different problems.
- Do not split one weakness only because the domain or language differs.
- Do not merge different root causes just because they both failed tests.
- `name` must be concise and reusable.
- `description` should explain the shared capability gap in 1-2 sentences.
- `covered_tags` should contain only tags that truly belong together.
- Prefer a small number of coherent groups over many fragmented ones.
- Do not collapse clearly different weaknesses into one bucket.
